<%= form_for(contribution) do |f| %>
  <% if contribution.errors.any? %>
    <%= render "shared/errors", errors: contribution.errors %>
    <!--<div id="error_explanation">-->
    <!--  <h2><%= pluralize(contribution.errors.count, "error") %> prohibited this event from being saved:</h2>-->

    <!--  <ul>-->
    <!--  <% event.errors.full_messages.each do |message| %>-->
    <!--    <li><%= message %></li>-->
    <!--  <% end %>-->
    <!--  </ul>-->
    <!--</div>-->
  <% end %>
  <%= f.hidden_field :event_id, :value => event.id  %>
  <div class='col-md-6 offset-md-3 mt-1'>
      <div class='card card-block'>
          <div class='row my-1'>
              <div class='col-xs-12'>
                  <%= f.label :amount %>
                  <%= f.number_field :amount, class: "form-control" %>
              </div>
          </div>
          <div class='row my-1'>
              <div class='col-xs-12'>
                  <%= f.label :comment %>
                  <%= f.text_area(:comment, rows: 6, class: 'form-control') %>
              </div>
          </div>
          <div class='row my-1'>
              <div class='col-xs-12 mb-1'>
                  <%= f.submit 'Submit Contribution', class: 'btn btn-success btn-square btn-block' %>
          </div>
              <div class='col-xs-12'>
                  <%= link_to 'Cancel', :back, class: 'btn btn-danger btn-square btn-block' %>
              </div>
          </div>
      </div>
  </div>
  
 
  
<% end %>